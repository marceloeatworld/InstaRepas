name: 🧪 Test d'installation de la clé SSH et des hôtes connus

on:
  workflow_dispatch:

jobs:
  setup-ssh:
    runs-on: ubuntu-latest

    steps:
    - name: 📦 Récupération du Code
      uses: actions/checkout@v4.2.1

    # Créer le répertoire .ssh avant d'y ajouter known_hosts
    - name: 🛠️ Créer le répertoire .ssh
      run: mkdir -p ~/.ssh

    # Générer dynamiquement l'entrée known_hosts en récupérant la clé publique du serveur
    - name: 🔑 Récupérer la clé publique de l'hôte distant
      run: |
        ssh-keyscan -t rsa ${{ secrets.REMOTE_HOST }} >> ~/.ssh/known_hosts

    - name: 🗝️ Installer la clé SSH
      uses: shimataro/ssh-key-action@v2.7.0
      with:
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        known_hosts: ~/.ssh/known_hosts

    - name: 🖥️ Tester la connexion SSH
      run: ssh -vvv -o StrictHostKeyChecking=yes -p ${{ secrets.REMOTE_PORT }} -T ${{ secrets.REMOTE_USER }}@${{ secrets.REMOTE_HOST }}
